{
  "id": "M070",
  "name": "Sort compression and tempfiles",
  "status": "active",
  "summary": "Sort BGZF/gzip behavior, tempdir behavior, and nproc behavior.",
  "goal": "Validate BGZF/gzip output, temporary file placement, and nproc behavior for sort.",
  "explicit_non_goals": [
    "Do not implement parse behavior.",
    "Do not implement downstream commands.",
    "Do not report performance claims without parity validation."
  ],
  "allowed_paths": [
    "src/cli.rs",
    "src/main.rs",
    "src/sort.rs",
    "tests/**",
    "docs/**",
    "scripts/benchmark_sort_threads.sh",
    "milestones/M070-sort-compression-and-tempfiles.json"
  ],
  "forbidden_paths": [
    "src/parse.rs"
  ],
  "allowed_commands": [
    "scripts/cargo_guard.sh check",
    "scripts/cargo_guard.sh test"
  ],
  "forbidden_commands": [],
  "allowed_pairtools_commands": [
    "pairtools sort"
  ],
  "forbidden_pairtools_commands": [
    "pairtools merge",
    "pairtools dedup",
    "pairtools select",
    "pairtools split",
    "pairtools stats"
  ],
  "oracle_commands": [
    "pixi run pairtools sort -o /tmp/oracle.sorted.pairsam.gz tests/fixtures/M070_sort_input.pairsam"
  ],
  "candidate_commands": [
    "pixi run cargo run -- sort --nproc 8 --tmpdir /tmp -o /tmp/candidate.sorted.pairsam.gz tests/fixtures/M070_sort_input.pairsam"
  ],
  "required_tests": [
    "scripts/cargo_guard.sh check",
    "scripts/cargo_guard.sh test"
  ],
  "optional_tests": [
    "python3 scripts/benchmark_sort_threads.sh --help"
  ],
  "required_benchmarks": [],
  "cargo_required_if_paths_changed": [
    "src/**",
    "tests/**",
    "Cargo.toml",
    "Cargo.lock",
    "pixi.toml",
    "pixi.lock",
    "build.rs"
  ],
  "docs_required": [
    "docs/STATUS.md",
    "docs/PAIRTOOLS_COMPATIBILITY.md"
  ],
  "status_fields_required": [
    "Active milestone",
    "Implemented behavior",
    "Intentionally unsupported behavior",
    "Validation performed",
    "Next recommended milestone"
  ],
  "acceptance_criteria": [
    "Compressed output decompresses with gzip-compatible tools.",
    "Temp files respect tmpdir.",
    "nproc behavior is validated without overclaiming performance."
  ],
  "known_risks": [
    "Compression throughput depends heavily on input and host environment."
  ],
  "next_milestone_candidates": [
    "M080"
  ]
}
