{
  "id": "M193",
  "name": "Sort custom columns memory semantics",
  "status": "planned",
  "summary": "Expand sort beyond default key and basic tmpdir behavior.",
  "goal": "Implement pairtools-compatible custom sort columns/options such as `--c1`, `--c2`, `--p1`, `--p2`, `--pt`, `--extra-col`, and documented memory semantics.",
  "explicit_non_goals": [
    "Do not broaden unrelated commands.",
    "Do not claim full pairtools parity beyond committed oracle fixtures.",
    "Do not run benchmarks or claim speedup.",
    "Pairtools may be used only as a test oracle.",
    "Do not weaken stable deterministic equal-key ordering."
  ],
  "allowed_paths": [
    "src/cli.rs",
    "src/main.rs",
    "src/sort.rs",
    "tests/**",
    "docs/**",
    "milestone_results/**",
    "milestones/ACTIVE_MILESTONE",
    "milestones/M193-sort-custom-columns-memory-semantics.json"
  ],
  "forbidden_paths": [
    "Cargo.toml",
    "Cargo.lock",
    "pixi.toml",
    "pixi.lock",
    "build.rs"
  ],
  "allowed_commands": [
    "scripts/cargo_guard.sh check",
    "scripts/cargo_guard.sh test"
  ],
  "forbidden_commands": [
    "cargo check",
    "cargo test",
    "cargo build",
    "pixi run cargo check",
    "pixi run cargo test",
    "pixi run cargo build"
  ],
  "allowed_pairtools_commands": [
    "pairtools sort"
  ],
  "forbidden_pairtools_commands": [
    "pairtools parse",
    "pairtools merge",
    "pairtools dedup",
    "pairtools select",
    "pairtools split",
    "pairtools stats",
    "pairtools flip",
    "pairtools markasdup",
    "pairtools filterbycov",
    "pairtools restrict",
    "pairtools sample",
    "pairtools header",
    "pairtools parse2",
    "pairtools phase",
    "pairtools scaling"
  ],
  "oracle_commands": [
    "pixi run pairtools sort <fixture-specific arguments>"
  ],
  "candidate_commands": [
    "target/debug/pairs-rs sort <fixture-specific arguments>"
  ],
  "required_tests": [
    "scripts/cargo_guard.sh check",
    "scripts/cargo_guard.sh test"
  ],
  "optional_tests": [],
  "required_benchmarks": [],
  "cargo_required_if_paths_changed": [
    "src/**",
    "tests/**",
    "benches/**",
    "examples/**",
    "Cargo.toml",
    "Cargo.lock",
    "pixi.toml",
    "pixi.lock",
    "build.rs"
  ],
  "docs_required": [
    "docs/STATUS.md",
    "docs/PAIRTOOLS_COMPATIBILITY.md"
  ],
  "status_fields_required": [
    "Active milestone",
    "Current branch",
    "Current commit",
    "Implemented behavior",
    "Intentionally unsupported behavior",
    "Validation performed",
    "Validation not performed and why",
    "Next recommended milestone"
  ],
  "acceptance_criteria": [
    "Oracle tests cover custom column names/indices and pair_type ordering.",
    "Memory option behavior is either implemented with documented semantics or rejected loudly.",
    "`--nproc` determinism remains covered for custom-key fixtures."
  ],
  "known_risks": [
    "Memory semantics may require approximation or explicit documented unsupported behavior."
  ],
  "next_milestone_candidates": [
    "M194",
    "M300"
  ]
}
