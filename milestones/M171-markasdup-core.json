{
  "id": "M171",
  "name": "Markasdup core",
  "status": "complete",
  "summary": "Implement scoped duplicate marking as a standalone command.",
  "goal": "Implement `pairs-rs markasdup` for pairs/pairsam streams, marking pair_type `DD` and SAM duplicate metadata where pairsam SAM columns exist.",
  "explicit_non_goals": [
    "Do not broaden unrelated commands.",
    "Do not claim full pairtools parity beyond committed oracle fixtures.",
    "Do not run benchmarks or claim speedup.",
    "Pairtools may be used only as a test oracle.",
    "Do not alter dedup clustering logic."
  ],
  "allowed_paths": [
    "src/cli.rs",
    "src/main.rs",
    "src/markasdup.rs",
    "tests/**",
    "docs/**",
    "milestone_results/**",
    "milestones/ACTIVE_MILESTONE",
    "milestones/M171-markasdup-core.json"
  ],
  "forbidden_paths": [
    "Cargo.toml",
    "Cargo.lock",
    "pixi.toml",
    "pixi.lock",
    "build.rs"
  ],
  "allowed_commands": [
    "scripts/cargo_guard.sh check",
    "scripts/cargo_guard.sh test"
  ],
  "forbidden_commands": [
    "cargo check",
    "cargo test",
    "cargo build",
    "pixi run cargo check",
    "pixi run cargo test",
    "pixi run cargo build"
  ],
  "allowed_pairtools_commands": [
    "pairtools markasdup"
  ],
  "forbidden_pairtools_commands": [
    "pairtools parse",
    "pairtools sort",
    "pairtools merge",
    "pairtools dedup",
    "pairtools select",
    "pairtools split",
    "pairtools stats",
    "pairtools flip",
    "pairtools filterbycov",
    "pairtools restrict",
    "pairtools sample",
    "pairtools header",
    "pairtools parse2",
    "pairtools phase",
    "pairtools scaling"
  ],
  "oracle_commands": [
    "pixi run pairtools markasdup <fixture-specific arguments>"
  ],
  "candidate_commands": [
    "target/debug/pairs-rs markasdup <fixture-specific arguments>"
  ],
  "required_tests": [
    "scripts/cargo_guard.sh check",
    "scripts/cargo_guard.sh test"
  ],
  "optional_tests": [],
  "required_benchmarks": [],
  "cargo_required_if_paths_changed": [
    "src/**",
    "tests/**",
    "benches/**",
    "examples/**",
    "Cargo.toml",
    "Cargo.lock",
    "pixi.toml",
    "pixi.lock",
    "build.rs"
  ],
  "docs_required": [
    "docs/STATUS.md",
    "docs/PAIRTOOLS_COMPATIBILITY.md"
  ],
  "status_fields_required": [
    "Active milestone",
    "Current branch",
    "Current commit",
    "Implemented behavior",
    "Intentionally unsupported behavior",
    "Validation performed",
    "Validation not performed and why",
    "Next recommended milestone"
  ],
  "acceptance_criteria": [
    "Oracle tests compare marked pairs/pairsam output against pairtools on small fixtures.",
    "Pairsam SAM duplicate flag and Yt tag behavior is validated where SAM columns exist.",
    "Unsupported markasdup options fail loudly."
  ],
  "known_risks": [
    "SAM field mutation must preserve unit-separator encoding used by pairsam."
  ],
  "next_milestone_candidates": [
    "M180",
    "M191"
  ]
}
