# Oracle Outputs

Place deterministic pairtools oracle outputs here.

Use names like:

- `M030_simple_uu.pairtools.out`

Each oracle file should be generated by a command recorded in the relevant milestone JSON or task report.
