# Fixtures

Place small SAM inputs and chrom-size files here.

Use names like:

- `M030_simple_uu.sam`
- `M030_simple_uu.chrom.sizes`

Do not add large binary files. Prefer the smallest fixture that proves the milestone behavior.
